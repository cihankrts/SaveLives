<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.cihan.hayatver">

    <uses-permission android:name="android.permission.INTERNET" />

    <application
        android:allowBackup="true"
        android:icon="@drawable/ikon"
        android:label="@string/app_name"
        android:roundIcon="@drawable/ikon"
        android:supportsRtl="true"
        android:theme="@style/Theme.HAYATVER">
        <activity android:name=".ChatActivity"></activity>
        <activity android:name=".LoginActivity1" />
        <activity android:name=".KanAriyanlar" />
        <activity android:name=".KanVerenKayit" />
        <activity android:name=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>